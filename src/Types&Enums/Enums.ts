export enum role{
    user = "user",
    manager = "manager",
    admin = "admin"
}

export enum listingStatus{
    available = "available",
    sold = "sold",
    cancelled = "cancelled"
}

export enum tradeStatus{
    pending = "pending",
    completed = "completed",
    cancelled = "cancelled"
}

export enum priority{
    urgent = "urgent",
    normal = "normal"
}


export {};